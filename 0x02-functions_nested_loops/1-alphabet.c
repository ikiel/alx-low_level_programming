#include "main.h"

/**
 *main - Entry point
 *A program thst prints the alphabet in lowercase
 *Return: Always 0 (Success)
 */

void print_alphabet(void);

int main(void)
{
print_alphabet();
return (0);
}
