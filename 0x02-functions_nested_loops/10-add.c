#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result
 * @a: first number
 * @b: second number
 * Return: sum of a and b
 */

int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
