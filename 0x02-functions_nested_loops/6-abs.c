#include "main.h"

/**
 * _abs - computes absolute value of an integer
 * @n: int in questuen
 * Return: the absolute value of integer
 */

int _abs(int n)
{
	if (int >= 0)
		return (n);
	else
		return (-n);
}

